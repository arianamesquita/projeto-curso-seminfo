import { ExemploDirective } from './exemplo.directive';

describe('ExemploDirective', () => {
  it('should create an instance', () => {
    const directive = new ExemploDirective();
    expect(directive).toBeTruthy();
  });
});
